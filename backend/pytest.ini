[pytest]
markers =
    tier1: Pure unit tests — no API calls, fast and deterministic
    llm: LLM integration tests — requires ANTHROPIC_API_KEY
    e2e: End-to-end pipeline tests — requires API keys and cached output data
asyncio_mode = auto
